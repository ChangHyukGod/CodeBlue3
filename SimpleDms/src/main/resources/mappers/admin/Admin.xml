<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.simplecoding.simpledms.mapper.admin.AdminMapper">

    <!-- 게시물 목록 조회 -->
    <select id="selectAdminList"
            parameterType="Criteria"
            resultType="Admin">
        SELECT *
        FROM TB_FAQ
        WHERE TITLE LIKE '%' || #{searchkeyword} || '%'f
        OFFSET #{pageIndex} * #{recordCountPerPage} ROWS FETCH NEXT #{recordCountPerPage} ROWS ONLY
    </select>

    <!-- 게시물 총 개수 조회 -->
    <select id="selectAdminListTotCnt"
            parameterType="Criteria"
            resultType="int">
        SELECT COUNT(*)
        FROM TB_FAQ
        WHERE TITLE LIKE '%' || #{searchkeyword} || '%'
    </select>

    <!-- 게시물 추가 -->
    <insert id="insert"
            parameterType="Admin">
        INSERT INTO TB_FAQ(FNO,
                           TITLE,
                           QUESTION,
                           ANSWER,
                           INSERT_TIME,
                           UPDATE_TIME,
                           FAQ_DATA,
                           FAQ_URL)
        VALUES (SQ_ADMIN.NEXTVAL,
                #{title},
                #{question},
                #{answer},
                #{insertTime},
                #{updateTime},
                #{faqData},
                #{faqUrl})
    </insert>

    <!-- 게시물 조회 -->
    <select id="selectAdmin"
            parameterType="int"
            resultType="Admin">
        SELECT *
        FROM TB_FAQ
        WHERE FNO = #{fno}  <!-- Assuming FNO as the primary key -->
    </select>

    <!-- 게시물 수정 -->
    <update id="update"
            parameterType="Admin">
        UPDATE TB_FAQ
        SET TITLE = #{title},
        QUESTION = #{question},
        ANSWER = #{answer},
        UPDATE_TIME = #{updateTime},
        FAQ_DATA = #{faqData},
        FAQ_URL = #{faqUrl}
        WHERE FNO = #{fno}
    </update>

    <!-- 게시물 삭제 -->
    <delete id="delete"
            parameterType="int">
        DELETE FROM TB_FAQ
        WHERE FNO = #{fno}
    </delete>

</mapper>
